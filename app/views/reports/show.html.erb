<div class="container">
  <header class="top-header py-3">
    <div class="row flex-nowrap justify-content-between align-items-center">
      <div class="col-4 text-center">
        <%= link_to 'Voltar à Lista', root_path, class: 'btn btn-primary text-white' %>
      </div>
      <div class="col-4 text-center">
        <h1>Relatório</h1>
      </div>
      <div class="col-4 text-center">
        <%= link_to 'Exportar para Excel', export_to_excel_report_path(@report), class: 'btn btn-success', method: :get %>
      </div>
    </div>
  </header>
</div>

<%= render 'filters' %>
<%= render 'document_info' %>
<%= render 'products' %>
<%= render 'taxes_and_totalizers' %>
